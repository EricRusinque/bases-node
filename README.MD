# Notes:

This is my first aplication in NodeJS

```
Options:
        --help     Show help                                                            [boolean]
        --version  Show version number                                                  [boolean]
    -b, --base     Is the base of the multiplication table                    [number] [required]
    -l, --list     Show the table in the console            [boolean] [required] [default: false]
    -t, --to       Is how far what number goes the table                                 [number]
```
